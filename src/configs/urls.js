const baseURL = 'http://owu.linkpc.net/api/v3';

const urls = {
  cars: '/cars',
  auth: {
    login: '/auth',
    refresh: '/auth/refresh',
  },
  users: '/users', // registration
};

export { baseURL, urls };
